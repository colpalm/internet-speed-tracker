[tool.poetry]
name = "internet-speed-tracker"
version = "0.1.0"
description = ""
authors = ["Colin Palmer <colinpalmer91@gmail.com>"]
readme = "README.md"
packages = [
    {include = "api"},
    {include = "database"},
    {include = "internet_speed_tracker"}
]

[tool.poetry.dependencies]
python = "^3.12"
sqlalchemy = "^2.0.38"
fastapi = "^0.115.11"
requests = "^2.32.3"
uvicorn = "^0.34.0"
dotenv = "^0.9.9"


[tool.poetry.group.dev.dependencies]
behave = "^1.2.6"
black = ">=22.1.0"
pytest = "^8.3.4"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
